# Copy to .env at the HitOrQuit root. These credentials are deployment-only.
AWS_ACCESS_KEY_ID=replace_with_access_key_id
AWS_SECRET_ACCESS_KEY=replace_with_secret_access_key
AWS_REGION=us-east-1

# Only required for temporary AWS credentials; leave empty otherwise.
AWS_SESSION_TOKEN=
